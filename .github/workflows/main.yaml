# Controls when the action will run. Triggers the workflow on push
on: 
  push:
    branches:
      - gha-build

jobs:
  build:
    name: Hello world action
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1

      - name: Testing simple step
        run: |
          echo test
